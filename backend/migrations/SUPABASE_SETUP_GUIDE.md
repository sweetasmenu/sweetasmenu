# Supabase Setup Guide - User Profiles Table

## 📋 สร้างตาราง user_profiles ใน Supabase

### ขั้นตอนที่ 1: เปิด Supabase Dashboard

1. ไปที่ https://app.supabase.com
2. เลือกโปรเจคของคุณ
3. คลิกที่ **SQL Editor** ทางซ้าย

### ขั้นตอนที่ 2: รัน Migration SQL

1. คลิก **New query**
2. Copy เนื้อหาทั้งหมดจากไฟล์ `backend/migrations/add_user_roles.sql`
3. Paste ลงใน SQL Editor
4. คลิก **Run** หรือกด `Ctrl+Enter`

### ขั้นตอนที่ 3: ตรวจสอบผลลัพธ์

หลังจากรัน SQL แล้ว:

1. ไปที่ **Table Editor** ทางซ้าย
2. ตรวจสอบว่ามาราง `user_profiles` ถูกสร้างขึ้นแล้ว
3. ตรวจสอบว่า:
   - มี column: `id`, `user_id`, `role`, `created_at`, `updated_at`
   - Foreign key ไปยัง `auth.users` ถูกสร้างแล้ว
   - Trigger `on_auth_user_created` ถูกสร้างแล้ว

### ขั้นตอนที่ 4: ตั้งค่า RLS Policies

RLS Policies จะถูกสร้างอัตโนมัติจาก migration script แต่ถ้ายังไม่มี:

1. ไปที่ **Authentication** > **Policies**
2. เลือกตาราง `user_profiles`
3. ตรวจสอบว่ามี policies:
   - Users can view own profile
   - Users can update own profile
   - Admins can view all profiles
   - Admins can update all profiles

### ขั้นตอนที่ 5: ทดสอบการทำงาน

#### สร้าง Admin User:

```sql
-- หา user_id ของคุณ
SELECT id, email FROM auth.users;

-- สร้าง admin profile (แทนที่ YOUR_USER_ID_HERE ด้วย user_id จริง)
INSERT INTO public.user_profiles (user_id, role)
VALUES ('YOUR_USER_ID_HERE', 'admin')
ON CONFLICT (user_id) DO UPDATE SET role = 'admin';
```

#### ทดสอบ Trigger:

1. สร้าง user ใหม่ใน Authentication
2. ตรวจสอบว่า `user_profiles` มี record ใหม่อัตโนมัติ
3. ตรวจสอบว่า role เป็น `free_trial`

---

## 🔗 การเชื่อมต่อกับ auth.users

ตาราง `user_profiles` เชื่อมต่อกับ `auth.users` ผ่าน:

1. **Foreign Key**: `user_id` → `auth.users(id)`
2. **ON DELETE CASCADE**: เมื่อลบ user ใน auth.users จะลบ profile อัตโนมัติ
3. **Trigger**: สร้าง profile อัตโนมัติเมื่อ user สมัครสมาชิก

---

## 📊 โครงสร้างตาราง

```
user_profiles
├── id (UUID, Primary Key)
├── user_id (UUID, Foreign Key → auth.users.id, UNIQUE)
├── role (TEXT, CHECK: 'free_trial' | 'starter' | 'professional' | 'enterprise' | 'admin')
├── created_at (TIMESTAMP)
└── updated_at (TIMESTAMP)
```

---

## ✅ Checklist

- [ ] ตาราง `user_profiles` ถูกสร้างแล้ว
- [ ] Foreign key ไปยัง `auth.users` ทำงานได้
- [ ] Trigger `on_auth_user_created` ทำงานได้
- [ ] RLS Policies ถูกตั้งค่าแล้ว
- [ ] ทดสอบสร้าง user ใหม่แล้ว (profile ถูกสร้างอัตโนมัติ)
- [ ] สร้าง admin user แล้ว

---

## 🐛 Troubleshooting

### ปัญหา: Foreign key constraint failed

**แก้ไข:**
- ตรวจสอบว่า `auth.users` มี user อยู่แล้ว
- ตรวจสอบว่า `user_id` เป็น UUID format ที่ถูกต้อง

### ปัญหา: Trigger ไม่ทำงาน

**แก้ไข:**
- ตรวจสอบว่า function `handle_new_user()` ถูกสร้างแล้ว
- ตรวจสอบว่า trigger `on_auth_user_created` ถูกสร้างแล้ว
- ลองรัน migration SQL อีกครั้ง

### ปัญหา: RLS Policy ไม่ทำงาน

**แก้ไข:**
- ตรวจสอบว่า RLS ถูก enable แล้ว: `ALTER TABLE user_profiles ENABLE ROW LEVEL SECURITY;`
- ตรวจสอบว่า policies ถูกสร้างแล้ว
- ตรวจสอบว่าใช้ Service Role Key ใน backend (ไม่ใช่ Anon Key)

---

## 📝 หมายเหตุ

- ตารางนี้อยู่ใน schema `public` (default)
- Foreign key ไปยัง `auth.users` ซึ่งอยู่ใน schema `auth`
- Trigger ทำงานอัตโนมัติเมื่อ user สมัครสมาชิกผ่าน Supabase Auth

